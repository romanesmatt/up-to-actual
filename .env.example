# up-to-actual — Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# SECURITY: Never commit .env to version control.

# ── Required ─────────────────────────────────────────────

# Up Bank personal access token (https://api.up.com.au)
UP_API_TOKEN=up:yeah:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Actual Budget server URL (your PikaPods instance)
ACTUAL_SERVER_URL=https://your-instance.pikapods.net

# Actual Budget password
ACTUAL_PASSWORD=your-actual-password

# Actual Budget sync ID (UUID — found in Settings > Advanced)
ACTUAL_SYNC_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Actual Budget account ID (run `npm run test:actual` to find this)
ACTUAL_ACCOUNT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ── Optional ─────────────────────────────────────────────

# End-to-end encryption password (only if you enabled E2E encryption)
# ACTUAL_E2E_PASSWORD=

# Local directory for Actual Budget data cache (default: ./actual-data)
# ACTUAL_DATA_DIR=./actual-data

# Webhook URL for notifications (Discord, Ntfy, Pushover, etc.)
# WEBHOOK_URL=https://discord.com/api/webhooks/xxx/yyy

# Sync window in hours (default: 48)
# SYNC_WINDOW_HOURS=48

# Max retry attempts on failure (default: 4)
# MAX_RETRIES=4

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=info
